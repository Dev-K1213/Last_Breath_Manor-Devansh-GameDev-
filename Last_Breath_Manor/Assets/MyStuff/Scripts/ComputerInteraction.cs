using UnityEngine;
using UnityEngine.UI;

public class ComputerInteraction : MonoBehaviour
{
/*    public GameObject timerDisplayUI; // UI object to show the timer
    private float timeSinceStart;
    private bool showTimer = false;

    void Start()
    {
        timeSinceStart = 0f;
        timerDisplayUI.SetActive(false);
    }

    void Update()
    {
        timeSinceStart += Time.deltaTime;

        if (showTimer)
        {
            timerDisplayUI.SetActive(true);
            timerDisplayUI.GetComponent<Text>().text = "Time Left: " + FormatTime(timeSinceStart);
        }
    }

    public void ShowTimer()
    {
        showTimer = true;
    }

    string FormatTime(float time)
    {
        int minutes = Mathf.FloorToInt(time / 60f);
        int seconds = Mathf.FloorToInt(time % 60f);
        return $"{minutes:00}:{seconds:00}";
    }
    */
}
